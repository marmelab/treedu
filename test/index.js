var assert = require('assert');
var should = require('should');

describe('Index', function() {
    describe('launch with node', function() {
        it('should display project resume', function() {
            var dummy =  true;
            dummy.should.be.true;
        });
    });
});
